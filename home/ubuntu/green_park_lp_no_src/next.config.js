/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Se você tiver imagens de domínios externos, configure aqui
  // images: {
  //   domains: ['example.com'],
  // },
}

module.exports = nextConfig

